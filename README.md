# modbus_rtu
modbus_rtu运行在freertos与裸机上

使用：
    1.将文件解压复制到keil工程中
    2.可以在modbus_config中配置映射表数据量和映射地址的起始地址
        ---地址排布是依次增加
    3.该文件不在意如何发送，只完成了部分modbus协议帧的字节处理
    4.发送在dev设备层抽象了一个发送函数，请尽量将自己的串口发送函数架构在这里，参数只有发送的数组和长度
    5.后续会更新，有空的话
注:
    写的很烂，但是希望对你有帮助。
    